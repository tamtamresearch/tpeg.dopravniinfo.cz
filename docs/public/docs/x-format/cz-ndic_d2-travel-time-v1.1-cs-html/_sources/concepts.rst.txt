========
Koncepty
========

Popisovaný formát umožňuje poskytování informací o dojezdových dobách osobních a nákladních vozidel na sadě předem definovaných tras.

Publikace typu ElaboratedDataPublication
========================================

Formát vychází z DATEX II publikace typu `ElaboratedDataPublication` [16157-5]_ a využívá ji pro přenos informací o dojezdové době.

.. literalinclude:: ../../samples/travel-time.xml
   :language: xml
   :linenos:
   :lineno-match:
   :end-before: <basicData

Dojezdová doba na konkrétní trase je uvedena v elementu `elaboratedData`, který se v publikaci může vyskytovat opakovaně, popisující dojezdové doby na různých trasách. Element `elaboratedData` nemá vlastní unikátní identifikátor a je jednoznačně určen časem publikace a identifikátorem odkazované polohy.


Popis trasy (předem definovaná trasa na silniční síti)
=======================================================

*Trasa* bývá popsána liniovým úsekem na silniční síti, daným většinou přítomností detekce průjezdu vozidel. Vzhledem k tomu, že se poloha senzorů snímajících vozidla, či úseky na kterých se dojezdové doby sledují, nemění, využívá tento profil pro popis polohy odkaz `LocationByReference` na předem definovanou polohu popsanou instancí třídy `PredefinedLocation` z balíčku `PredefinedLocationsPublication`. Trasa tedy není popsána přímo v souboru. 

Trasa stavové informace (úseku dojezdové doby) je tedy vyjádřena odkazem do sady předem definovaných míst. Pro dohledání místa je proto nutno získat publikaci `PredefinedLocationsPublication` související s tímto zdrojem informací a v něm dohledat polohu shodného typu (dle `targetClass`, zde `PredefinedLocation`), se stejným identifikátorem a verzí.

Trasa, pro kterou vypočtená dojezdová doba platí, je specifikována pomocí elementu `pertinentLocation`. Ten má vždy `xsi:type="LocationByReference"`. Samotný odkaz je proveden vnořeným elementem `predefinedLocationReference` s atributem `targetClass="PredefinedLocation"`.

.. literalinclude:: ../../samples/travel-time.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <basicData
   :end-before: <vehicleType

Dojezdová doba osobních a nákladních vozidel
============================================

*Dojezdová doba* je vyjádřena v sekundách pomocí atributu `travelTime` typu `DurationValue` ve třídě `TravelTimeData`.

Je vyjádřena jen aktuálně vypočítaná dojezdová doba, ostatní údaje (ideální dojezdová doba, běžná dojezdová doba, rychlost při ideálních podmínkách, trend dojezdové doby) nejsou použity.

Profil poskytuje pouze data vypočítaná pro osobní, nákladní vozidla a bez rozlišení.

.. literalinclude:: ../../samples/travel-time.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: </pertinentLocation>
   :end-before: /basicData>

Poskytování pouze právě dostupných dat
======================================

Formát přenáší dojezdové doby pouze pro ty trasy, pro které jsou dostupné údaje, ostatní jsou vynechány.

===============
Časová platnost
===============

Platnost dojezdových dob je uvedena jednou pro všechny záznamy v publikaci pomocí elementu `timeDefault` typu `DateTime` třídy `ElaboratedDataPublication`. 

Uvedený čas platnosti vyjadřuje aktuální čas ke kterému se vypočítaná data vztahují, jedná se o konec periody měření.

Žádné další určení platnosti údajů není používáno.

.. literalinclude:: ../../samples/travel-time.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <payloadPublication
   :end-before: <headerInformation>

================
Popisování poloh
================

Formát popisuje polohy výhradně odkazem do sady předem definovaných poloh (třída `PredefinedLocation`) v publikaci `PredefinedLocationsPublication` [16157-3]_.

Metody popisu polohy jsou mimo rámec tohoto dokumentu a jsou popsány jinde.

