========
Koncepty
========

Popisovaný formát umožňuje poskytování předdefinovaných poloh, které využívají publikace stavových událostí (dojezdové doby, hustota provozu, situace v tunelu, obsazenost parkovišť atp.)

Publikace typu PredefinedLocationPublication
============================================

Formát vychází z DATEX II publikace typu `PredefinedLocationPublication` z 
[16157-3]_. Profil využívá většiny datových struktur, který tato
publikace nabízí a navíc ji rozšiřuje o vlastní popisy poloh.

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :end-before: <predefinedLocationContainer

Popis předdefinovaných poloh
============================

Každý `predefinedLocationContainer` typu `xsi:type="PredefinedLocation"` obsahuje popis polohy jednoho reálného místa. V publikaci je tolik elementů `predefinedLocationContainer` kolik je předdefinovaných poloh. Jednoznačnost předdefinovaných poloh v této publikaci je zaručena unikátním identifikátorem a verzí.

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: </headerInformation>
   :end-before: <predefinedLocationName>

V publikaci je možné použít pro popis více nespojitých poloh dohromady `predefinedLocationContainer` typu `xsi:type="PredefinedNonOrderedLocationGroup"`, každá samostatná poloha je potom obsažena ve vnořených elementech `predefinedLocation`

.. literalinclude:: ../../samples/predefined_location_loc_group.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: </headerInformation>
   :end-before: <predefinedLocationName>

   
Poloha může být popsána bodem nebo krátkým liniovým úsekem na silniční síti.  Nepoužívají se polohy typu oblast. 

Způsoby popisu polohy jsou dále popsány v kapitole "Popisování poloh".


===============
Časová platnost
===============

Doba od kdy záznamy uvedené v publikaci platí, je uvedena hromadně pro všechny předdefinované polohy dobou vydání publikace, tedy elementem `publicationTime`. 

.. note::
	Nově vydaná publikace **vždy obsahuje i ty předešlé**, nové mají nové id a aktualizace vyšší verzi. Uvnitř záznamů `predefinedLocationContainer` není žádný časový záznam uveden.

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <feedType
   :end-before: <publicationCreator

================
Popisování poloh
================

Všechny polohy v této publikaci `PrefefinedLocationPublication` jsou předem definované a každá je uvedena v elementu `PrefefinedLocationContainer` na jehož id a verzi se odkazují polohy událostí z jiných publikací. Při popisu předdefinovaných poloh se využívají instance třídy `Location`, konkrétně potomci třídy `NetworkLocation`, jmenovitě buď třídou `Point`, nebo `Linear`. 

K popisu polohy je možné použít 3 metody pro popis liniové lokace a 4 metody pro popis bodové lokace v závislosti na jejich dostupnosti v dané lokalitě. Musí být uveden alespoň jeden popis.

Popis polohy pomocí ALERT-C
===========================

Metoda popisuje polohu pomocí ALERT-C kódů, třídou `alertCLinear` typu `AlertCMethod2Linear`. Jiné typy popisu ALERT-C se nepoužívají. Použití této metody závisí na přítomnosti ALERT-C lokace v daném místě.

Metoda `alertCLinear` používá k popisu události primární lokaci (element `alertCMethod2PrimaryPointLocation`), sekundární lokaci (element `alertCMethod2SecondaryPointLocation`). Metoda samozřejmě obsahuje i prvky umožňující identifikaci konkrétní lokační tabulky.

Primární lokace je dle principu ALERT-C místo kde došlo k nehodě, či kde řidič opouští problémovou oblast. Sekundární lokace je konec kolony či místo, kde řidič poprvé vjíždí do problémové oblasti. 

Aby bylo možné zjistit směr dané situace je použit element `alertCDirectionCoded`, který nabývá hodnoty `positive` v případě že je směr dopravního proudu souhlasný s kódováním dané routy. 

.. important::

	POZOR, význam tohoto atributu je tedy přesně opačný než atribut `direction` v RDS-TMC, který slouží k dohledání sekundární lokace.

Element `alertCLinear` se může v popisu situace vyskytnout vícekrát a to pro dlouhé události, které musí být popsány více než jednou ALERT-C cestou. ALERT-C totiž umožňuje řetězit lokace pouze v rámci jedné cesty.

.. note::

	Pro popis **bodové lokace** (i vnitřek křižovatky či tunelu, tedy krátký úsek) se používá také metoda `alertCLinear`. Kdy jsou primární a sekundární lokace stejné. Směr je možné vyčíst z elementu `alertCDirectionCoded`).

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <!-- Sample of alertCLinear begin -->
   :end-before: <!-- Sample of alertCLinear end -->

Popis polohy pomocí staničení
=============================

Metoda používá pro popis liniové polohy staničení `linearWithinLinearElement`. Element `roadNumber` popisuje číslo ovlivněné komunikace, element `directionRelativeOnLinearSection` popisuje směr ovlivněné komunikace a elementy `fromPoint` a `toPoint` popisují vzdálenost počátku a konce události od počátku silnice. 

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <!-- Sample of linearWithinLinearElement begin -->
   :end-before: <!-- Sample of linearWithinLinearElement end -->

Metoda používá pro popis bodové polohy staničení `pointAlongLinearElement` se liší tím, že pouze popisuje jednu vzdálenost od počátku identifikované komunikace místo dvou.

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <!-- Sample of pointAlongLinearElement begin -->
   :end-before: <!-- Sample of pointAlongLinearElement end -->
   
.. note::

	Pro popis **bodové lokace** (i vnitřek křižovatky či tunelu, tedy krátký úsek) se běžně používá také metoda  `linearWithinLinearElement`, kdy ofsety `fromPoint` a `toPoint` odkazují na úsek s nulovou délkou. 

Popis polohy pomocí rozšíření Global Network
============================================

Nativní způsob zadávání polohy v NDIC využívající **rozšíření DATEX II modelu úrovně B**.

Metoda popisuje, v elementu `globalNetworkLinear`, polohu pomocí segmentů `linearWithinLinearGNElement` silniční sítě Global Network (GN), identifikované verzí `networkVersion`. Každý segment obsahuje pořadí, identifikátor odkazující do GN, a relativní zkrácení (pro první a poslední úsek). Dále je poloha ohraničena elementy `startPoint` a `endPoint` v souřadnicích (zde S-JTSK). 


.. note::

	Pro popis **bodové lokace** (i vnitřek křižovatky či tunelu, tedy krátký úsek) se používá také metoda  `globalNetworkLinear`. Polohy `startPoint` a `endPoint` poté obsahují stejnou souřadnici. 

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <!-- Sample of globalNetworkLinear begin -->
   :end-before: <!-- Sample of globalNetworkLinear end -->

Popis polohy pomocí OpenLR
==========================

Metoda OpenLR v elementu `openlrExtendedLinear`, používá pro popis polohy routovací algoritmus a tím umožňuje tzv. on-the-fly lokalizaci.

Stanovuje referenční body `openlrLocationReferencePoint` a `openlrLastLocationReferencePoint` pomocí souřadnic, směru silnice, a typu silnice. Tyto body jsou použity jako začátek a konec pro nalezení cesty která je dále zpřesněna pomocí ofsetů na skutečný rozsah události. V případě, že nestačí 2 body metoda umožňuje požít mezilehlé body. 

.. note::

	Pro popis **bodové lokace** (i vnitřek křižovatky či tunelu, tedy krátký úsek) se používá také metoda  `openlrExtendedLinear`, kdy ofsety odkazují na úsek s nulovou délkou.

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <!-- Sample of openlrExtendedLinear begin -->
   :end-before: <!-- Sample of openlrExtendedLinear end -->

Popis polohy pomocí souřadnic
=============================

Metoda umožňuje popsat bodovou lokaci pomocí souřadnic referenčního systému WGS-84 elementem `pointByCoordinates`. 

.. literalinclude:: ../../samples/predefined_location_samples_set.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <!-- Sample of pointByCoordinates begin -->
   :end-before: <!-- Sample of pointByCoordinates end -->

