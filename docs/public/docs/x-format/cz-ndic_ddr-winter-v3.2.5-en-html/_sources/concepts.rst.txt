========
Concepts
========

This section highlights main concepts, used by this format. For complete
description refer to schemas with definitions in comments.

In this section are provided some snippets of real XML documents. For more
examples, see :ref:`examples`

XML Document Sections
=====================

The XML Document is built using following elements and sections:

- DOC (document root)
- INF (metadata)
- MJD (container for messages)
- MSG (message)

Document root (`DOC`)
---------------------

The `DOC` element represents single publication (one instance of information message).

Attribute `id` provides unique identity to given publication as a whole.

.. literalinclude:: ../../samples/new-snow-cover.xml
   :language: xml
   :linenos:
   :end-before: <MSG

Metadata (`INF`)
----------------

`INF` element provides meta-information about whole publication:

- reference to GDF 4.0 map data set publisher and version
- reference to state organization units and areas enumeration UIR-ADR

.. literalinclude:: ../../samples/new-snow-cover.xml
   :language: xml
   :linenos:
   :start-after: extended
   :end-before: <MSG

Message container (`MJD`)
-------------------------

Single traffic messages are put into one parent element `MJD`. It can contain one or more messages.


Traffic Message (`MSG`)
-----------------------

Particular message is represented by a `MSG` element. 

The message itself has following sections:

- validity `MTIME`
- textual description of whole event `MTXT`
- weather description `MEVT/WCOND`
- road conditions description `MEVT/MTNCOND`
- location reference `WDEST` (*where*) to enumeration of winter conditions
  report region
- reference to a region in Czech Republic `MDST`

.. literalinclude:: ../../samples/new-snow-cover.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <MJD
   :end-before: </MJD>

Message identity and version
^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Message itself has unique identifier `id` and version. Version shall start from
`1` and be incremented by each update.

Validity of the message in time is specified by simple time range from start time to end time, but in fact only start time is relevant.

.. literalinclude:: ../../samples/new-snow-cover.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <MJD
   :end-before: <MTXT

Weather description
^^^^^^^^^^^^^^^^^^^

The `MEVT/WCOND` element contains set of meteorological parameters
(temperature, precipitation, wind, visibility etc.)

.. literalinclude:: ../../samples/new-snow-cover.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: <MEVT>
   :end-before: <MTNCOND>

Driving Conditions description
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

The `MEVT/MTNCOND` element contains parameters , describing driving conditions.

.. literalinclude:: ../../samples/new-snow-cover.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: </WCOND>
   :end-before: </MEVT>

Location description
^^^^^^^^^^^^^^^^^^^^

DDR (Winter) is using references to areas, from which are information provided.

There are following location referencing methods used:

- Reference to enumeration of report regions (see :ref:`regioncodes`)
- Coordinate of centre of report region
- Reference to region in Czech Republic using enumeration UIR-ADR.

.. literalinclude:: ../../samples/new-snow-cover.xml
   :language: xml
   :linenos:
   :lineno-match:
   :start-after: </MEVT>
   :end-before: </MSG>

.. _regioncodes:

Enumeration of report regions
=============================

Report regions are defined in `WinterConditionsRegionCodes.rnc`, each having a code and in a name.

For complete list see :ref:`regioncodes-complete`.

.. literalinclude:: ../../schema/WinterConditionsRegionCodes-en.rnc
   :language: xml
   :linenos:
   :lineno-match:
   :end-before: "11"

